name: Minimal Workflow Runner for Debugging

on:
  workflow_dispatch:

jobs:
  test-run:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello World! If you see this message, the basic workflow trigger is working correctly."
